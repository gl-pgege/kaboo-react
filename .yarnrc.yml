nodeLinker: node-modules

# Yarn's default registry (registry.yarnpkg.com) is a read-only npm mirror and
# rejects publishes, so target npm directly for publishing.
npmPublishRegistry: "https://registry.npmjs.org"
